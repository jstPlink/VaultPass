# Per costruire l'immagine dal codice invece di scaricarla:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  vaultpass:
    image: vaultpass:local
    build: .
